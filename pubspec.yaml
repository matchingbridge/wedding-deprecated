name: wedding
description: A new Wedding project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  get: ^4.3.8
  dio: ^4.0.0
  shared_preferences: ^2.0.6
  image_picker: ^0.8.4+4
  video_player: ^2.2.5

dev_dependencies:
  # flutter_test:
  #   sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/